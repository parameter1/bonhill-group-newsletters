import defaultValue from "@parameter1/base-cms-marko-core/utils/default-value";

$ const { linkUrl } = input;
$ const linkText = defaultValue(input.linkText, 'READ MORE');

$ const linkStyle = {
  "color": "#ffffff",
  ...input.linkStyle,
};

<common-link-element
  link-text=linkText
  link-url=linkUrl
  link-style=linkStyle
/>
